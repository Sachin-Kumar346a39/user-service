message: 'Hello from local test'
spring:
  application:
    name: user-service
    
  datasource:
    url: jdbc:h2:mem:localhost
    username: admin
    password: root
    
  jpa:
    show-sql: true  
  h2:
    console:
      enabled: true 
  
server:
  port: 9090
   